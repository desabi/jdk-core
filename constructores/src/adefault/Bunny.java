package adefault;

public class Bunny {
    // Default constructor
    // Is supplied if there are no constructor present.

    // When a constructor is defined, the default no-argument
    // constructor is not inserted automatically
}
